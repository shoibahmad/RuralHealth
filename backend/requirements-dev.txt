# Development and test tooling. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

# Test suite
pytest>=8.4,<10.0
pytest-django>=4.11,<5.0
pytest-cov>=6.0,<8.0

# Lint and format
ruff>=0.9,<1.0

# Dependency vulnerability auditing (also run in CI)
pip-audit>=2.7,<3.0
